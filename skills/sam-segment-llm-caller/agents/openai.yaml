interface:
  display_name: "SAM Segment LLM Caller"
  short_description: "Call OCR+SAM UI targeting APIs."
  default_prompt: "Choose between /v1/ui/click-targets, /v1/ui/segment, and /v1/sam/segment/text-points based on user intent, and always include required text_mode (`screen_text` for labels, `visual` for objects)."
